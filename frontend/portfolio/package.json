{
  "name": "@example/minimal",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "source run.sh && astro dev",
    "start": "astro dev",
    "build": "source run.sh -p && astro build && touch dist/.nojekyll",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^0.21.13"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.0",
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.6.1"
  }
}
