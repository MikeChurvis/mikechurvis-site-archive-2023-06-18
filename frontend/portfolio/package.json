{
  "name": "@example/minimal",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "source make-env.sh ../../env && astro dev",
    "start": "source make-env.sh ../../env && astro dev",
    "build": "source make-env.production.sh ../../env && astro build && touch dist/.nojekyll",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^0.21.13"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.0",
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.6.1"
  }
}
